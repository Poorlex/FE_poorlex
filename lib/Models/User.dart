class UserInfo {
  String? userId;
  String? userName;

  UserInfo({ this.userId = null, this.userName = null });
}

class UserToken {
  String? token;
  String? refreshToken;

  UserToken({ this.token = null, this.refreshToken = null });
}